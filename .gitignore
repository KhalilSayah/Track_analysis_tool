__pycache__/
*.pyc
# API Keys and Secrets
.env
service-account-key.json
backend/service-account-key.json
venv/
.streamlit/
.DS_Store
# Ignore data files
*.csv
# Ignore generated reports in test folder, but keep the folder
test/*.pdf
test/*.md
test/*.png
!test/analysis_report.md
# (User might want to keep the sample report, so I will not ignore it for now, or maybe I should. 
# The user has analysis_report.md in test, let's assume they might want to commit sample outputs. 
# But usually we ignore outputs. I'll ignore pdfs and pngs in root or test generated by runs.)
